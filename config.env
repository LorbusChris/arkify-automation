# Global configuration for arkify-automation.
# The kernel repository that holds all target/infra/consumption branches.
# The DEPLOY_KEY secret must be a write-enabled deploy key on this repo.
KERNEL_REPO_SLUG=LorbusChris/linux
